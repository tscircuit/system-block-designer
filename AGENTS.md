# Agent notes

## Testing

- **One test per file.** Don't put multiple `test(...)` in one file; split them.
- **Name a test file after what it verifies** (kebab-case). Avoid generic,
  misleading names like `browsertest` — the name should say what's tested.
